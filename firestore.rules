
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Aberto para desenvolvimento e depuração v3.2
      allow read, write: if true;
    }
  }
}
