
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    /**
     * REGRAS DE ACESSO SIMPLIFICADAS
     * Permissão total para desenvolvimento e testes rápidos.
     */
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
