
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    /**
     * REGRAS SIMPLIFICADAS
     * Permissão total para facilitar o desenvolvimento e testes em equipe reduzida.
     */
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
